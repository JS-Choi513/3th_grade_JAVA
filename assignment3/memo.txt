조건식을 따질때 short circuit 를 잘 따져라 
조건식은 왼쪽에서 오른쪽으로 연산하며 판별하는데
왼쪽이 거짓이면 오른쪽은 연산하지 않음 

for-each 문 
배열이나 나열의 각 원소를 순차적으로 접근하는데 유요한 for ans

int[] num = {12345};l;
in sum = 0;
for(int k:num)//반복 될 때마다 k 는 num[0] `..1..2... 값으로 설정
sum +=k;

문자열은 for( String S; names)

세개 입력 받고 
그 값이 각각 1보다 크고 10보다 작은지 확인 
곱해질 두 배열의 인덱스를 선언한다
각 배열의 인덱스 만큼 숫자를 입력받는다 
입력받는 숫자가 0<=n<=100 인지 확인 
곱셉값이 저장되는 배열의 인덱스 크기는 첫재 행렬 행*둘째행렬 열 선언

한원소당 연산횟수는 n(열의 갯수만큼)회

2차원 배열은 1차원배열을 원소로 하는 배열이다.
배열선언 
int[][] intArray;
int intArrat[][]= {{0,1,2},{3,4,5}}

2차원 배열에서 길이측정(length)할 때 
2행 5열의 배열 i 에서 
i.length -> 이면 이차원 배열의 행의 개수를 반환 =2
i.length[n] -> 이면 n 번째 행의 열의 개수 반환 = 5 물론 n번행의 열 개수가 p n+1번 열의 개수가 h 일수 도 있다, 

비정방형 배열

물론 각 행의 열의 개수가 다른 배열도 생성 할 수 있다. 

메소드의 배열 리턴 
배열의 레퍼런스 리턴 
메소드의 리턴 타입
메소드의 리턴 타입과 리턴받는 배열 타입과 일치
리턴 타입에 배열의 크기를 지정하지 않음

예)
int[] makeArray() {
    int temp[] = new int[4];
    return temp;
}



